{% extends 'base.html.twig' %}

{% block title %}Lista de artículos {% endblock %}

{% block body %}
    <h1>Artículos relacionados con el usuario {{ usuario.username }}</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Username</th>
            <td>{{ usuario.username }}</td>
        </tr>
        <tr>
            <th></th>
            <td> {{ articulo. usuario_id }}</td>
        </tr>

        </tbody>
    </table>

    <a href="{{ path('usuario_show', {'id': usuario.id}) }}">Back to {{ usuario.username }} </a><br>

{% endblock %}
