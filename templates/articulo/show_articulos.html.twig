{% extends 'base.html.twig' %}

{% block title %}Articulos{% endblock %}

{% block body %}
    <h2>Articulos escritos por usuario ""</h2>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Titulo</th>
            <th>Contenido</th>
            <th>Fecha de creación</th>
            <th>Autor</th>
            <th>Ver los detalles</th>
            <th>Editar</th>
        </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ articulo.id }}</td>
                <td>{{ articulo.titulo }}</td>
                <td>{{ articulo.contenido }}</td>
                <td>{{ articulo.creationDate ? articulo.creationDate|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('usuario_show', {'id': articulo.usuario.id}) }}">{{ articulo.usuario }}</a>

                </td>
                <td>
                    <a href="{{ path('articulo_show', {'id': articulo.id}) }}">ver</a>
                </td>
                <td>
                    <a href="{{ path('articulo_edit', {'id': articulo.id}) }}">editar</a>
                </td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('articulo_new') }}">Crear nuevo articulo</a>
{% endblock %}
